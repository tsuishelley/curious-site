# Contentful — copy to .env.local and fill in your values
# Space ID found in Contentful → Settings → API keys
CONTENTFUL_SPACE_ID=

# Content Delivery API access token (read-only, safe to use server-side)
CONTENTFUL_ACCESS_TOKEN=

# Preview API token — only needed if you want draft content in preview mode
CONTENTFUL_PREVIEW_ACCESS_TOKEN=
